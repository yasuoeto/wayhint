# common — wayhint で何ができるか

- 形式: 画面録画 + 字幕のみ（無音 or BGM）。ナレーション無し
- **4 本のうちの 1 本**。この showcase は製品の共通機能だけを扱い、context の**見つけ方**は
  `herdr` / `terminal` / `gui` の 3 本が経路ごとに受け持つ。全体を通した 1 本は撮らず、
  4 本の出力を章タイトルで繋いで作る（`demo/showcases/all/`）
- 尺の variant は持たない。短い版は素材を切り出すのではなく、**別の showcase として撮り直す**
- 主役の 3 場面（この順で出す）
  1. **端末の中で動いているものまで見て自動で切り替わる**
  2. **focus を奪わないので、出したまま作業を続けられる**
  3. **忘れた操作をその場で追記して育てる**
- 表記: hotkey は実機の割り当て `Super+H`（toggle）/ `Super+Ctrl+H`（編集モード）/
  `Super+Shift+H`（検索モード）に合わせる

---

## 0. 撮影前の準備

> 手で録るのではなく、隣の `02_common_scenario.yaml` から
> `./scripts/demo --showcase common --record` で生成する（DECISIONS 0032）。hint sheet は
> `demo/fixtures/hints/ja/`、端末と Herdr の設定は `demo/fixtures/` と `tools/demo/session.py`、
> 録画は headless の compositor の中。**下の表の秒と字幕は、実際に生成されるものと同期して
> ある**（同期を保つのは人。`demo/README.md`「字幕の書き方」9）。撮れない演出（ブラウザ、ズーム、マウス操作、ロゴ画面）は
> `demo/README.md` の「台本から落とした場面」に理由付きで並べてある。

### 環境

- 録画は自分専用の headless compositor の中で行う。いま使っているセッションには触らない
- 映すアプリ: Herdr + Claude Code / Codex の stub、vi の stub
- **映さない**: 実コード、会話内容、ホームパスに含まれるユーザー名
- Claude Code / Codex は起動直後の待ち受け画面で止める（stub なので会話は起きない）

### なぜ Herdr の中で撮るのか

この showcase の主語は**共通機能**で、Herdr はその舞台にすぎない。それでも舞台を 1 つ選ぶ必要が
あり、pane を切り替えるだけで「中で動いているもの」が変わる Herdr がいちばん短く見せられる。
どうやってそこまで辿り着いているか——Herdr に聞く、`/proc` を読む、app_id で選ぶ——は
`herdr` / `terminal` / `gui` の 3 本の仕事で、ここでは説明しない。

### hint sheet（`demo/fixtures/hints/ja/`）

- `herdr.yaml` / `claude-code.yaml` / `codex.yaml` — 実機の `~/.config/wayhint/hints/ja/` の写し
  (2026-09-24、文言も手を加えていない)。Herdr 6 件、Claude Code 7 件、Codex 1 件。tag の絞りは
  どこにも書いていないので、子の一覧には Herdr の hint が全部混ざる(DECISIONS 0034)。`favorite` は
  無いので ★ は出ない
- `wm.yaml` — `match` 無し、2 件。`config.yaml` に `include: [wm]`
- `vi.yaml` — §6 で YAML を開く場面のもの。`notes.yaml` / `less.yaml` は他の showcase が使う
- 撮影中に追記する hint は「直前の検索を繰り返す / `n`」。検索で選ばれた Herdr の hint の隣、
  つまり Herdr の sheet に入る

## 1. 本編
<!-- variant: main -->

16:9、実際の操作テンポ。合計 192 秒。

### §1 課題(0:00–0:18)

| 秒 | 画面 | 字幕 |
|---|---|---|
| 0–3 | Herdr の窓 | Herdr の中で Claude Code を動かしている |
| 3–8 | Claude Code が立ち上がる | 操作を忘れるたびに、Web で検索する |
| 8–13 | 同じ画面 | 調べたことが、どこにも残らない |
| 13–18 | 同じ画面 | 欲しいのは、いま動いているもののヒント |

### §2 いつも同じ場所(0:18–0:32)

画面は 3 枚とも同じ（overlay が出たまま）。字幕を読み切れる長さに詰めてある。

| 秒 | 画面 | 字幕 |
|---|---|---|
| 18–24 | `Super+H`。右上に overlay | hotkey 一発で、フォーカス中のウィンドウのヒント。位置は設定可能 |
| 24–28 | 一覧(左 key / 中央 title と command / 右 category) | 左にキー、中央に説明（あればコマンド）、右に分類 |
| 28–32 | 選択中の行の詳細が下に出ている | 補足情報は下に出る |

### §3 主役1: 中まで見て切り替わる(0:32–0:58)

| 秒 | 画面 | 字幕 |
|---|---|---|
| 32–37 | ヘッダー `Herdr › Claude Code` | 見ているのはウィンドウではなく、中のプロセス |
| 37–42 | 新しいタブ → Codex | (字幕なし) |
| 42–50 | `Super+H` → `Herdr › Codex` に差し替わる | 別のペインで押し直せば、閉じずに差し替わる |
| 50–58 | 一覧に Herdr の hint(スクロール、tab、ペースト)が混ざっている | 親子関係のプロセスは自動でヒントを混ぜる、Herdr の操作も並ぶ |

### §4 主役2: 奪わない(0:58–1:40)

コピーの場面は Claude Code のペインで撮る。コピーされるのは `copy`、無ければ `command` だけで
(DECISIONS 0039)、Codex と Herdr のシートは key しか持たない。

| 秒 | 画面 | 字幕 |
|---|---|---|
| 58–61.5 | 最初のタブ(Claude Code)に戻り、`Super+H` で `Herdr › Claude Code` に差し替わる | (字幕なし) |
| 61.5–69.5 | overlay を出したまま端末に `write tests` と打つ(ASCII: 全角だと `Ctrl+U` がプロンプトまで消す) | 通常表示中はキーボードを奪わない |
| 69.5–70.5 | `Ctrl+U` で端末の行を消す | (字幕なし) |
| 70.5–73 | `Super+Shift+H` → 検索欄が開き、下部に検索中のキー | 検索は Super+Shift+H |
| 73–80 | 検索欄に「圧縮」→ 一覧が 1 件(`/compact`)に絞られる | キーボードの入力先を切り替えるのは、検索を始めたときだけ |
| 80–84.5 | `↓` → 一覧へ。下部に `c コピーして戻る · Enter/Esc 戻る …` | ↓ で一覧へ。使えるキーは下に出る |
| 84.5–89 | `c` → `/compact` をコピーして検索を抜ける。一覧は 1 件のまま、chip が出る | c で選択中のヒントのコマンドをコピーして戻る |
| 89–94.5 | `Ctrl+Shift+V` → 端末に `/compact` が貼られる。一覧は絞られたまま | 絞り込みは残り、入力は元のウィンドウへ |
| 94.5–100 | chip の `×` → 一覧が全件に戻る | 絞り込みはシートごとに残る。解除は × |

### §5 主役3: その場で育てる(1:40–2:18)

| 秒 | 画面 | 字幕 |
|---|---|---|
| 100–105 | `Super+Ctrl+H`。下部に編集モードの操作一覧 | 忘れていた操作を見つけたら、その場で書く |
| 105–109 | `a` → フォーム。見出しは「新しいヒント → Claude Code」 | 追加先は、いま選んでいるヒントと同じシート |
| 109–116.6 | タイトル → Tab → キー | (字幕なし) |
| 116.6–123.6 | Enter。一覧に新しい行が増える | Enter で保存すると編集モードは終わる |
| 123.6–130.6 | sheet の YAML を壊す → `⚠ YAML エラー`、一覧は残る | 手で編集して壊しても、最後に読めた内容が残る |
| 130.6–137.6 | 直す → ⚠ が消える | 直せばそのまま復帰する。再起動は要らない |

### §6 まとめと仕組み(2:18–2:58)

vi の stub は引数の sheet を実際に開く。字幕が指す `match` は画面のその行にあり、言語は status 行の
`"hints/ja/claude-code.yaml"` に出る。`include` はこの sheet には書いておらず(実機の写しのため)、
`config.yaml` の `include: [wm]` で全 sheet に混ざっている——字幕はその書き方を言う。親子関係の
プロセスの絞り(`nested.export_tags`)は `herdr` showcase が扱う。

| 秒 | 画面 | 字幕 |
|---|---|---|
| 137.6–143.6 | `Super+H` で閉じる | 忘れた瞬間、いつも同じ場所に、必要な情報がある |
| 143.6–150.6 | vi で `hints/ja/claude-code.yaml` を開く | シートは 1 アプリ 1 YAML |
| 150.6–158.6 | 同じ画面(5–7 行目の `match:`) | match でどのウィンドウ・どのプロセスに出すかが決まる |
| 158.6–166.6 | 同じ画面(この sheet に `include:` は無い。wm は config.yaml の `include` で混ざる) | 共通のヒントは config.yaml の include で全シートに混ぜる |
| 166.6–174.6 | 同じ画面(status 行の `hints/ja/`) | ヒントは言語ごと。UI と同じ言語が出る |
| 174.6–177.6 | vi の窓を閉じる | (字幕なし) |

### §7 締め(2:58–3:12)

| 秒 | 画面 | 字幕 |
|---|---|---|
| 177.6–185.6 | overlay を閉じた画面 | やらないこと: コマンドの実行・AI 生成・画面の読み取り |
| 185.6–191.6 | 同じ画面 | Wayland (labwc) / GTK4 / ヒントは YAML |

## 2. 字幕の書き方メモ

規約は `demo/README.md`「字幕の書き方」が正。ここで守るのはその 7 番——主役 3 場面の見出し字幕で、
他の showcase が同じ場面に触れるときも同じ文言を使う。

- 見ているのはウィンドウではなく、中のプロセス
- 通常表示中はキーボードを奪わない
- 忘れていた操作を見つけたら、その場で書く

## 3. 撮影後のチェック

- [ ] 画面にユーザー名・実コード・会話内容が映っていない
- [ ] `contact-sheet.png` で行が切れていない
- [ ] 撮影で追記した hint が fixtures に残っていない（session の copy に書くので残らないはず）
- [ ] 字幕帯(`demo/README.md`「字幕帯」の表の値)にウィンドウと overlay がかかっていない
