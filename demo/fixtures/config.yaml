overlay:
  anchor: top-right
  width: 560px
  height: 84%
  margin: {top: 24, right: 24}
appearance:
  style: style.css
  language: ja
editor:
  command: [gvim, --remote-silent, "+{line}", "{file}"]
include: [wm]
search:
  max_results: 30
logging:
  level: info
